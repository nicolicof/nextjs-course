import { Inter, Lusitana } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] })
export const lusitana = Lusitana({ weight: ["400", "700"], subsets: ['latin'] })

// Lusitana(options: { weight: "400" | "700" | Array<"400" | "700">; style?: "normal" | Array<"normal">; display?: Display; variable?: undefined; preload?: boolean; fallback?: string[]; adjustFontFallback?: boolean; subsets?: Array<"latin">; }): NextFont
